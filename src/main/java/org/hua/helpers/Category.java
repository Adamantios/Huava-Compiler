package org.hua.helpers;

/**
 * Enumeration containing all the possible existing categories in the input code of the compiler.
 */
public enum Category{
    TYPE,
    STATEMENT,
    METHOD,
    KEYWORD,
    IDENTIFIER,
    NUMBER,
    STRING,
    OPERATOR,
    AFTERDOT
}
